<hotdeploy>
  
  <fileurlprefix name="file:"/>
  
  <webappdir name="/eclnthotdeploy/classes"/>
  <webappdir name="/eclnthotdeploy/lib"/>
  
  <!--
  <dir name="c:/xyz/xyz"/>
   -->
</hotdeploy>

<!--
Configuration for the optional hot deployment framework. The directories 
that are listed + their containing jar files are added to the classpath
of the hot deployment classloader.

The configuration below is an example only!

...by webappdir: in this case the direcotry is located below the
web application. This is only possible in servlet containers unpacking
the web application (e.g. Tomcat)

...by dir: this is a direct directory definition
 -->